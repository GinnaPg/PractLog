//USER PROFILE


//HIGHEST NUMBER

let userNumber = [];
 userNumber = prompt ("Escribe 10 numeros, separalos usando comas.")

 let numeros = userNumber.split(",").map(mumero);
 let max = numeros[0];

 for (let i = 1; i < numeros.length; i++);
 if (numeros[i] > max){
    max = numeros[i];
 }
console.log("El numero mas grande es:" + max);
//ALARM
//PALINDRONE
//FACTORIAL
//FLAT ARRAY